android.newDsl=false
android.useAndroidX=true
org.gradle.jvmargs=-Xmx1536M
# Keep this explicit so Flutter's compatibility migrator does not opt the
# AGP 9 example back out of built-in Kotlin.
android.builtInKotlin=true
